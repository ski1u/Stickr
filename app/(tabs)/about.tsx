import "@/global.css"

import { Text, View } from "react-native"

export default function About() {
    return (
        <View className="flex-1 justify-center items-center">
            <Text>About Page WIP.</Text>
        </View>
    )
}